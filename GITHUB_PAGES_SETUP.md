# 🚀 Развертывание блога на GitHub Pages

Пошаговая инструкция как опубликовать твой блог на GitHub Pages.

## Шаг 1: Создай репозиторий на GitHub

1. Зайди на https://github.com
2. Нажми "New repository" (зеленая кнопка)
3. Название репозитория: `твой-ник.github.io` (например: `ivanov.github.io`)
   - **Важно:** Если хочешь чтобы сайт был на `https://твой-ник.github.io/`
   - Или используй любое название для `https://твой-ник.github.io/название/`
4. Сделай репозиторий **Public**
5. Нажми "Create repository"

## Шаг 2: Загрузи файлы на GitHub

### Вариант А: Через Git (рекомендуется)

```bash
# Инициализируй Git (если еще не сделал)
git init

# Добавь все файлы
git add .

# Первый коммит
git commit -m "Initial commit: Мама я вайбкодер блог"

# Добавь удаленный репозиторий (замени на свой)
git remote add origin https://github.com/твой-ник/твой-репозиторий.git

# Залей на GitHub
git push -u origin main
```

### Вариант Б: Через веб-интерфейс GitHub

1. На странице репозитория нажми "uploading an existing file"
2. Перетащи всю папку `docs/` в окно браузера
3. Напиши commit message: "Add blog files"
4. Нажми "Commit changes"

## Шаг 3: Настрой GitHub Pages

1. Зайди в настройки репозитория (Settings)
2. В левом меню найди "Pages"
3. В разделе "Source":
   - **Branch:** выбери `main` (или `master`)
   - **Folder:** выбери `/docs`
4. Нажми "Save"

## Шаг 4: Подожди пару минут

GitHub Pages развертывается 1-3 минуты. Ты увидишь зеленую галочку и ссылку на сайт.

Твой сайт будет доступен по адресу:
- `https://твой-ник.github.io/` (если репозиторий `твой-ник.github.io`)
- `https://твой-ник.github.io/название-репо/` (если другое название)

## Шаг 5: Публикация новых постов

### Автоматический способ (рекомендуется):

1. Запусти генератор поста:
```bash
generate_post.bat
```

2. После генерации скрипт спросит: "Опубликовать на сайт?"
3. Нажми `y` - пост автоматически добавится в `docs/`

4. Залей изменения на GitHub:
```bash
git add .
git commit -m "Новый пост: название поста"
git push
```

5. Через 1-2 минуты пост появится на сайте!

### Ручной способ:

```bash
# Публикуешь конкретный пост
python publish_to_site.py generated_posts/название_поста.md

# Заливаешь на GitHub
git add .
git commit -m "Добавлен пост: название"
git push
```

## Структура проекта

```
MAMA_VIBE/
├── docs/                      # GitHub Pages сайт
│   ├── index.html            # Главная страница
│   ├── post-template.html    # Шаблон поста
│   ├── css/
│   │   └── style.css         # Темный дизайн
│   ├── js/
│   │   └── app.js            # Логика сайта
│   └── posts/
│       ├── posts.json        # Список всех постов
│       ├── post1.html        # Опубликованный пост
│       └── post2.html        # Опубликованный пост
├── generated_posts/           # Сгенерированные MD посты
├── post_generator.py         # Генератор постов
├── publish_to_site.py        # Публикатор на сайт
└── post_topics.xlsx          # Темы постов
```

## Workflow (как это работает)

```
1. Пишешь идеи в post_topics.xlsx
         ↓
2. Запускаешь generate_post.bat
         ↓
3. AI генерирует пост в generated_posts/
         ↓
4. Соглашаешься опубликовать на сайт (y)
         ↓
5. Пост конвертируется в HTML и добавляется в docs/
         ↓
6. git add, commit, push
         ↓
7. Пост на сайте! 🎉
```

## Полезные команды Git

```bash
# Проверить статус
git status

# Посмотреть изменения
git diff

# Добавить все файлы
git add .

# Коммит с сообщением
git commit -m "Описание изменений"

# Залить на GitHub
git push

# Скачать изменения
git pull
```

## Кастомизация дизайна

Хочешь изменить цвета или стиль? Отредактируй `docs/css/style.css`:

```css
:root {
    --bg-primary: #0a0a0a;      /* Основной фон */
    --bg-card: #1a1a1a;         /* Фон карточек */
    --accent: #7c3aed;          /* Акцентный цвет */
    --text-primary: #ffffff;    /* Основной текст */
}
```

## Свой домен (опционально)

Хочешь использовать свой домен (например, `myblog.com`)?

1. Купи домен (на Namecheap, Google Domains и т.д.)
2. В настройках домена добавь A-записи:
```
185.199.108.153
185.199.109.153
185.199.110.153
185.199.111.153
```
3. В корне `docs/` создай файл `CNAME` с твоим доменом:
```
myblog.com
```
4. Закоммить и залей на GitHub

## Проблемы?

### Сайт не открывается
- Подожди 5 минут после первой публикации
- Проверь что в Settings → Pages выбран правильный Branch и `/docs`

### Посты не появляются
- Проверь что `posts.json` обновился
- Проверь что HTML файлы постов в `docs/posts/`
- Проверь консоль браузера (F12) на ошибки

### Стили не применяются
- Проверь пути в `index.html` и шаблоне постов
- Если репозиторий не `твой-ник.github.io`, нужно добавить базовый путь

---

**Готово!** Теперь у тебя есть свой блог на GitHub Pages с темным дизайном! 🚀

Вопросы? Пиши в чат "Мама я вайбкодер" 😎

