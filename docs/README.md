# 🚀 Мама я вайбкодер - GitHub Pages сайт

Современный блог с темным дизайном для публикации постов про вайбкодинг.

## Структура

```
docs/
├── index.html              # Главная страница с карточками постов
├── post-template.html      # Шаблон для генерации страниц постов
├── css/
│   └── style.css          # Темный дизайн
├── js/
│   └── app.js             # Логика сайта (загрузка постов, поиск)
└── posts/
    ├── posts.json         # Список всех постов (JSON)
    └── *.html             # Опубликованные посты
```

## Дизайн

- 🌙 Темная тема
- 📱 Адаптивный (mobile-friendly)
- 🎨 Современные карточки с анимацией
- 🔍 Встроенный поиск
- ⚡ Быстрая загрузка

## Как это работает

1. **Генерация постов**: Запускаешь `generate_post.bat`
2. **Публикация**: Скрипт `publish_to_site.py` конвертирует MD → HTML
3. **Обновление списка**: Автоматически добавляет пост в `posts.json`
4. **Git push**: Заливаешь на GitHub → сайт обновляется

## Кастомизация

### Изменить цвета

Отредактируй переменные в `css/style.css`:

```css
:root {
    --bg-primary: #0a0a0a;      /* Основной фон */
    --bg-card: #1a1a1a;         /* Фон карточек */
    --accent: #7c3aed;          /* Акцентный цвет (фиолетовый) */
    --text-primary: #ffffff;    /* Основной текст */
}
```

### Изменить название блога

В `index.html` найди:

```html
<h1 class="logo">🚀 Мама я вайбкодер</h1>
<p class="tagline">Вайбкодинг • AI • Автоматизация контента</p>
```

### Изменить шаблон поста

Редактируй `post-template.html`

## Развертывание

Полная инструкция в корне проекта: `GITHUB_PAGES_SETUP.md`

---

Сделано с ❤️ для вайбкодеров

